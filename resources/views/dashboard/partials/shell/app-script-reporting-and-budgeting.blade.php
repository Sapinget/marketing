@verbatim
                const { saveBonusConfig } = window.MarketingDashboardRuntimeHelpers.createReportingOperations({
                    bonusConfig,
                    showBonusSettings,
                    ensureRunApi,
                    showNotification,
                    notifyError,
                });
@endverbatim
